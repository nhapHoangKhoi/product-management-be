mixin search-form(keyword)
   form(form-search="")
      .input-group 
         input(
            class="form-control"
            type="text"
            placeholder="Search..."
            autocomplete="off"
            name="inputKeyword"
            value=keyword
         )
         .input-group-append 
            button(class="btn btn-success" type="submit") Search